var fs=require('fs');
var archivo="./prueba/file.txt";
fs.mkdir("prueba",(err)=>{
    if (err) {
        console.log("No se pudo crear la carpeta prueba");
    }else{
        // crear un arhivo dentro del directorio 
        fs.
    }
});
